# serenitymojo.ops.parity — DEV-ONLY parity oracles + fixtures for ops.
#
# Holds the numpy oracle (gen_moe_reference.py) and the generated Mojo data
# fixture (moe_ref_data.mojo) for the MoE op parity smoke. No Python at runtime:
# numpy is the offline oracle, the fixture is plain Mojo data.
