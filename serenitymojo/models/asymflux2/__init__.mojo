# serenitymojo.models.asymflux2 — AsymFLUX.2 Klein 9B asymmetric layer.
# The Klein backbone (models/dit/klein_dit.mojo) is reused verbatim as the
# `u_a` producer; this package holds only the ASYMMETRIC AsymFlow velocity
# reconstruction (asymflow.mojo) that wraps the transformer's proj_out.
