# Serenity Trainer Mojo UI — Deep Map (build spec for a web replacement)

Date: 2026-07-05
Scope: everything under `src/serenity_trainer/ui/` (34 files, 5904 lines), the
terminal-launch scripts, and the pixi UI build/run tasks. This document is the
completeness-over-brevity reference for a browser + Rust-server replacement of
the native MojoUI trainer screen. All citations are `file:line`.

The UI is a pure-Mojo immediate-mode app built on the `mojoui_floor` library
(`/home/alex/MojoUI`), rendered against an X11/framebuffer backend, that emits
per-model JSON config files and launches campaign-verified `serenitymojo`
trainer binaries in a spawned `gnome-terminal`, then tails their stdout progress
into a live right-rail. There is no in-process training; the UI is a
config-editor + launcher + progress-tailer + media-gallery.

---

## 1. FILE INVENTORY

All paths relative to `src/serenity_trainer/ui/`.

### Core (wired into the running app)

- **TrainerConfigModel.mojo** — 1701 lines. The config model + preset engine +
  all JSON emission. Defines `TrainerUIConfig` (the ~180-field editable state),
  `TrainerUIConcept`, `TrainerUISample`, the 13-entry `architecture_options`
  list, `trainer_ui_apply_model_preset` (the per-architecture preset table),
  `trainer_ui_runner_train_config_json` (per-backend runner config JSON),
  `trainer_ui_config_json_snapshot` / `trainer_ui_load_config_snapshot`
  (session persistence), the capability-table lever functions
  (`trainer_ui_supported_lever_keys`, `trainer_ui_active_lever_keys`,
  `trainer_ui_ignored_lever_summary`), and JSON-escaping helpers. Also holds all
  `comptime` path constants for every model's checkpoint/cache/VAE/sample-prompt.
  **This is the single most important file for the web port.**

- **TrainerRuntimeBridge.mojo** — 1238 lines. The launch + process-lifecycle +
  progress-parse layer. Defines `TrainerUIRuntime` (live run state),
  `TrainerUILiveStats` (step/loss/gpu numbers), `trainer_ui_submit_current`
  (Start), `trainer_ui_launch_live_runner` (argv construction + spawn),
  `_live_runner_command` (the 4 argv shapes), `trainer_ui_cancel`/`_pause`/
  `_resume`/`_sample_now`/`_save_checkpoint_now`, the progress-file tailer
  (`trainer_ui_poll_progress_file`, `trainer_ui_apply_progress_line`,
  `trainer_ui_apply_serenity_callback_line`), ETA math, and system-metrics refresh.

- **TrainUI.mojo** — 574 lines. The app entry point + `mojoui_floor` frame loop.
  Defines `TrainUIAppState` (owns `Context`, `cfg`, `runtime`, `media`,
  `captioner`, and 10 `TextEditState`s), `main()` (Backend init + `run_blocking`),
  `_frame()` (per-frame callback), `_ui()` (3-column shell layout), `_sidebar`
  (nav routing), `_main_panel` + `_main_section` (tab dispatch), `_status_rail`
  (right live rail), and inline `_backup_tab`/`_runs_tab`/`_logs_tab`.

- **TrainerMediaGallery.mojo** — 621 lines. Dataset/sample gallery state +
  texture streaming. Defines `TrainerMediaGalleryState`, gallery scan/load
  (`_load_dataset_gallery`, `_load_sample_gallery`), virtual-scroll texture
  windowing (`ensure_visible_dataset_textures`/`_sample_`, `_ensure_media_range`),
  throttled sample-dir rescan (`_refresh_sample_gallery`), the lightbox
  (`render_media_lightbox`, `open_dataset_preview`, `open_sample_preview`), the
  UTF-8-safe `_basename`, and video-thumbnail path handling.

- **TopBar.mojo** — 92 lines. `render_top_bar`: the action bar (SECTION label,
  run-name text edit, live-target label, Start/Stop, VALIDATION + WARNING line,
  Pause/Resume/Sample[not wired]/Save-checkpoint buttons).

- **UITabCommon.mojo** — 66 lines. Layout helpers: `row1`..`row6` (column-width
  lists), `two_col_w`, `value_w`. Pure geometry, no state.

### Config tabs (wired via `_main_section`)

- **GeneralTab.mojo** — 97 lines. `render_general_tab`: WORKSPACE (paths,
  cache policy), DEBUG & VALIDATION (SerenityBoard/tensorboard), DEVICE
  (placement/precision), MULTI-GPU panels.
- **ModelTab.mojo** — 76 lines. `render_model_tab`: BASE MODEL (method/model-type/
  architecture selectors — the preset triggers — + base-model edit) and OUTPUT
  panels. Calls `trainer_ui_apply_model_preset` on selector change (ModelTab.mojo:61-62).
- **LoraTab.mojo** — 62 lines. `render_lora_tab`: ADAPTER PARAMETERS (algorithm,
  rank, alpha, dropout, weight dtype) and TARGETS & OFT panels.
- **DatasetTab.mojo** — 108 lines. `render_dataset_tab`: DATASET path/resolution/
  cache toggles, PREVIEW & CAPTIONS summary, and the dataset `media_virtual_grid`.
  Returns the clicked tile index.
- **CaptionerTab.mojo** — 409 lines. `render_captioner_tab` + `CaptionerScreenState`.
  Recursive media scan, Qwen-VL caption-model selectors, prompt controls, sidecar
  discovery, command-file emission (`target/serenity_captioner_commands.jsonl`),
  and its own media grid. **Contains 3 crash-class byte-index sites (see §8).**
- **SamplingTab.mojo** — 96 lines. `render_sampling_tab`: SAMPLING SETTINGS
  (dir/cadence/steps/cfg/sampler), SAMPLE PROMPTS list, and the sample `media_virtual_grid`.
- **TrainingTab.mojo** — 200 lines. `render_training_tab`: the densest tab —
  BASE SCHEDULE, OPTIMIZER, PRECISION & MEMORY, EMA & TARGETS, TEXT ENCODER,
  TRANSFORMER, NOISE & TIMESTEPS, MASKED TRAINING [not wired], LOSS, LAYER FILTER.
- **ConceptTab.mojo** — 34 lines. `render_concepts_tab`: "Validations" —
  concept list + detail (read-only field rows).
- **CloudTab.mojo** — 32 lines. `render_cloud_tab`: CLOUD (SSH/RunPod target) +
  SYNC panels. All read-only field rows except the cloud-type select + delete toggle.

### Aux/window surfaces (NOT wired into the running app)

These are 1:1 Rust-snapshot parity surfaces. They are imported ONLY by
`tests/ui/test_aux_screens_compile.mojo` (a compile gate) — none are reachable
from the sidebar `_main_section`. They render mostly hardcoded `field_row`
placeholder text (see §8 "hardcoded" note). Treat them as future-tab stubs.

- **AuxScreenCommon.mojo** — 28 lines. `aux_panel_h`, `aux_label_w` shared geometry.
- **AdditionalEmbeddingsTab.mojo** — 29 lines. `render_additional_embeddings_tab`.
- **CaptionUI.mojo** — 33 lines. `render_caption_ui` (dataset caption-tool stub).
- **ConceptWindow.mojo** — 34 lines. `render_concept_window`.
- **ConfigList.mojo** — 21 lines. `render_config_list(title, rows)` generic list.
- **ConvertModelUI.mojo** — 31 lines. `render_convert_model_ui`.
- **GenerateCaptionsWindow.mojo** — 25 lines. `render_generate_captions_window`.
- **GenerateMasksWindow.mojo** — 25 lines. `render_generate_masks_window`.
- **MuonAdamWindow.mojo** — 26 lines. `render_muon_adam_window`.
- **OffloadingWindow.mojo** — 22 lines. `render_offloading_window`.
- **OptimizerParamsWindow.mojo** — 34 lines. `render_optimizer_params_window`.
- **ProfilingWindow.mojo** — 33 lines. `render_profiling_window`.
- **SampleFrame.mojo** — 30 lines. `render_sample_frame` (shared by sample windows).
- **SampleParamsWindow.mojo** — 9 lines. thin wrapper over `render_sample_frame`.
- **SampleWindow.mojo** — 27 lines. `render_sample_window` (+ live sample status).
- **SchedulerParamsWindow.mojo** — 31 lines. `render_scheduler_params_window`.
- **TimestepDistributionWindow.mojo** — 24 lines. `render_timestep_distribution_window`.
- **VideoToolUI.mojo** — 35 lines. `render_video_tool_ui`.
- **__init__.mojo** — 1 line. Package init comment.

---

## 2. ARCHITECTURE

### The mojoui_floor frame loop

`main()` (TrainUI.mojo:557) constructs a single `TrainUIAppState`, stores a raw
pointer to it via `store_user_state(sp)` (TrainUI.mojo:559-560), calls
`Backend.init(w, h, "Serenity Trainer")` (TrainUI.mojo:563), then
`Backend.run_blocking(_frame)` (TrainUI.mojo:573). `_frame` is the C-callback the
floor calls once per frame (60 Hz target).

`_frame()` (TrainUI.mojo:534) each frame:
1. Lazily loads the font on first frame (TrainUI.mojo:536-538).
2. `load_preview_textures(media, cfg, runtime)` — gallery scan / throttled rescan
   (TrainUI.mojo:540).
3. `_sync_window` — re-reads window size, reapplies theme + shell density
   (TrainUI.mojo:541, 143-152).
4. `trainer_ui_tick_and_apply(runtime)` — the progress/liveness pump
   (TrainUI.mojo:542).
5. `ctx.begin_frame` → `Backend.frame_begin(bg)` → `_ui(s)` → `ctx.end_frame` →
   `render_context_commands(ctx)` → `Backend.frame_end()` (TrainUI.mojo:543-554).
   Exceptions in `_ui`/`_render` are caught and printed, never fatal.

`_ui()` (TrainUI.mojo:512) lays out the 3-column shell in one row:
`layout_row(row5(nav_w, gap, main_w, gap, status_w), win_h)` (TrainUI.mojo:515),
then `_sidebar`, `_main_panel`, `_status_rail` each inside `begin_column`/
`end_column`, and finally `render_media_lightbox` as an overlay
(TrainUI.mojo:516-527).

### `TrainUIAppState` fields (TrainUI.mojo:78-127)

- `ctx: Context` — the mojoui immediate-mode context.
- `cfg: TrainerUIConfig` — the editable config (loaded from snapshot at init,
  TrainUI.mojo:108).
- `runtime: TrainerUIRuntime` — live run state.
- `font_id: UInt32`, `win_w`/`win_h: Float32`, `metrics: TrainerShellMetrics`.
- `main_scroll_y`, `status_scroll_y: Float32` — scroll offsets.
- `media: TrainerMediaGalleryState`, `captioner: CaptionerScreenState`.
- Ten `TextEditState` fields — the ONLY editable text inputs in the app:
  `run_name_edit`, `workspace_edit`, `cache_edit`, `base_model_edit`,
  `output_dir_edit`, `dataset_path_edit`, `sample_output_edit`,
  `captioner_folder_edit`, `captioner_custom_model_edit`, `captioner_prompt_edit`
  (TrainUI.mojo:91-100). Everything else is a select/slider/drag/toggle bound
  directly to a `cfg` field.

### Section / tab routing

- Section identity is `cfg.section_index: Int32` against the `UI_SECTION_*`
  comptime constants (TrainerConfigModel.mojo:6-17): GENERAL=0, MODEL=1, LORA=2,
  DATASET=3, CONCEPTS=4, TRAINING=5, SAMPLING=6, BACKUP=7, CLOUD=8, RUNS=9,
  LOGS=10, CAPTIONER=11.
- `_sidebar` (TrainUI.mojo:167) draws the "Serenity / Native trainer UI" header,
  a CONFIGURE group of `_nav` rows, a PROJECT group showing `cfg.run_name`. Nav
  order (TrainUI.mojo:187-198): General, Model, LoRA/OFT, Dataset, Captioner,
  Validations, Training, Sampling, Backup, Cloud, Runs, Logs.
- `_nav` (TrainUI.mojo:155) draws one `nav_row`; on click sets
  `cfg.section_index` and resets `main_scroll_y` (TrainUI.mojo:160-163).
- `_main_section` (TrainUI.mojo:299) is the big dispatch: it maps `section_index`
  to the tab renderer. DATASET and SAMPLING also call
  `ensure_visible_*_textures` before rendering and `open_*_preview` after, using
  the click index the tab returns (TrainUI.mojo:306-351). BACKUP/RUNS/LOGS are
  inline functions (`_backup_tab`, `_runs_tab`, `_logs_tab`).
- Section label text comes from `cfg.section_label()` (TrainerConfigModel.mojo:686);
  note it maps CONCEPTS→"Validations" and the label uses "LoRA / OFT".

### Window sizing / theme

- Initial size `_initial_window_size` (TrainUI.mojo:130): 92% × 90% of display,
  floored at `INIT_W=1480 × INIT_H=920` (TrainUI.mojo:74-75).
- Per-frame `_sync_window` (TrainUI.mojo:143) recomputes `TrainerShellMetrics`
  via `trainer_shell_metrics(w, h)`, applies `apply_serenity_trainer_theme(ctx)`
  (from `mojoui.theme.trainer_theme`) and `apply_shell_density(ctx, metrics)`.
  The status rail temporarily clamps font/row height (TrainUI.mojo:403-405) and
  restores them at the end (TrainUI.mojo:508-509).

### Launch environment requirements (the X11 coupling a web UI eliminates)

- **Build:** `pixi run trainer-ui-build` links `TrainUI.mojo` against
  `/home/alex/MojoUI` with `-lmojoui_floor -lm` (pixi.toml:117), `depends-on`
  `build-mojoui` (a `make -C /home/alex/MojoUI/c_floor`, pixi.toml:15) and
  `ideogram4-live-trainer-build` (pixi.toml:19).
- **Run:** `pixi run trainer-ui` = `LD_LIBRARY_PATH=/home/alex/MojoUI
  target/serenity_trainer_ui` (pixi.toml:118). The floor `.so` must be on
  `LD_LIBRARY_PATH`.
- **Display:** the app needs an X11/GNOME session. `Backend.display_size()` and
  `window_size()` drive layout. `Backend.system_metrics()` supplies GPU/CPU stats
  (TrainerRuntimeBridge.mojo:740).
- **Terminal spawn:** the launcher hard-requires a GNOME desktop
  (`gnome-terminal`, D-Bus, `XDG_RUNTIME_DIR`, `DISPLAY`) — see §4. It hardcodes
  `DISPLAY=:1`, `XDG_RUNTIME_DIR=/run/user/1000`, the D-Bus bus address, and a
  CUDA `LD_LIBRARY_PATH` for the spawned trainer
  (scripts/serenity_terminal_launcher.sh env block).

A web UI removes: the X11 backend, the MojoUI floor + textures, the
`gnome-terminal` spawn, and all `DISPLAY`/`XAUTHORITY`/`LD_LIBRARY_PATH` coupling.
The server would spawn trainer processes directly (no terminal window) and stream
their stdout over a socket.

---

## 3. CONFIG MODEL (the heart)

### `TrainerUIConfig` — every field, type, default, editing tab

`__init__` at TrainerConfigModel.mojo:341-684. Fields are grouped below with
default (from `__init__`) and the tab whose widget edits them. "read-only" means
the tab shows it as a `field_row` (display only) — not editable in the native UI,
but the web UI should surface it. Dropdown-backed fields store BOTH an options
`List[String]` and an `Int32` index (or a `String` current value).

#### Section / identity
| field | type | default | edited by |
|---|---|---|---|
| section_index | Int32 | UI_SECTION_MODEL (1) | sidebar nav |
| backend_target | String | "klein" | preset (derived, not user-typed) |
| run_name | String | "boxjana_klein9b_lora_v1" | TopBar run-name edit |

#### Dropdown option lists + indices (all initialized in `__init__`)
| field | options (index→label) | default index | edited by |
|---|---|---|---|
| training_method_options / _index | LoRA(0), Fine Tune(1), Embedding(2) | 0 | Model "Train Method [LoRA only]" |
| model_type_options / _index | see §3 arch table (13) | 1 (FLUX_2) | Model "Model Type" |
| architecture_options / _index | see §3 arch table (13) | 1 (Klein 9B) | Model "Architecture" |
| optimizer_options / _index | ADAMW8BIT(0), ADAMW(1), CAME(2), ADAFACTOR(3), MUON(4), SCHEDULE_FREE_ADAMW(5), AUTOMAGIC3(6) | 1 | Training "Optimizer" |
| scheduler_options / _index | COSINE(0), CONSTANT(1), LINEAR(2) | 0 | Training "Scheduler" |
| precision_options / _index | BFLOAT_16(0), FLOAT_16(1), FLOAT_32(2) | 0 | (used by several dtype selects) |
| cloud_type_options / _index | NONE(0), RUNPOD(1), LINUX(2) | 0 | Cloud "Cloud Type" |
| output_format_options | SAFETENSORS, CKPT, INTERNAL, DIFFUSERS | (string field) | Model "Format" |
| device_options | cuda, cpu | (string field) | General/Training device selects |
| lr_scaler_options | NONE, LINEAR, SQRT, COSINE | (string field) | Training "LR Scaler" |
| ema_options | OFF, EMA | (string field) | Training "EMA" |
| timestep_distribution_options | UNIFORM, LOGIT_NORMAL, MODE, COSINE, SIGMOID | (string) | Training "Distribution" |
| loss_weight_options | MIN_SNR_GAMMA, NONE, P2, DEBIASED_ESTIMATION | (string) | Training "Weight Fn" |
| loss_scaler_options | NONE, MIN_SNR_GAMMA, P2 | (string) | Training "Loss Scaler" |
| layer_filter_preset_options | full, attention, mlp, double_blocks, single_blocks | (string) | Training "Preset" |
| peft_options | LORA, LOCON, LOKR, LOHA, DORA, OFT | (string) | LoRA/Training "Algorithm" |
| sample_sampler_options | Ideogram4 FlowMatch, FlowMatch Euler, Euler, DDIM, DPM++ 2M, UniPC | (string) | Sampling "Sampler" |
| resolution_options | 512, 768, 1024, 1280 | (string) | Dataset/Training "Resolution" |
| select_open_id | String | "" | internal (which dropdown is open) |

#### Captioner fields (Captioner tab)
| field | type | default |
|---|---|---|
| captioner_model_options / _index | Qwen/Qwen3.5-4B, Qwen3.5-9B, Qwen3-VL-4B-Instruct, Qwen3-VL-8B-Instruct(3), Qwen2.5-VL-3B-Instruct, Qwen2.5-VL-7B-Instruct, Custom... | index 3 |
| captioner_custom_model_id | String | "" (edit_row when "Custom...") |
| captioner_quant_options / _index | None, 8-bit(1), 4-bit | 1 |
| captioner_attention_options / _index | flash_attention_2(0), eager | 0 |
| captioner_resolution_options / _index | auto(0), auto_high, fast, high | 0 |
| captioner_folder_path | String | SERENITY_BOXJANA_DATASET_DIR |
| captioner_prompt | String | "Describe this media." |
| captioner_skip_existing | Bool | True |
| captioner_summary_mode | Bool | False |
| captioner_one_sentence_mode | Bool | False |
| captioner_retain_preview | Bool | True |
| captioner_max_tokens | Float32 | 128.0 |

#### General tab
| field | type | default |
|---|---|---|
| debug_mode | Bool | False |
| debug_dir | String | "debug" |
| workspace_dir | String | "/home/alex/trainings/boxjana_klein9b_lora_v1" |
| cache_dir | String | SERENITY_BOXJANA_KLEIN_CACHE |
| tensorboard | Bool | True |
| tensorboard_expose | Bool | False |
| tensorboard_always_on | Bool | False |
| tensorboard_port | String | "6006" |
| validation | Bool | False |
| continue_last_backup | Bool | False |
| prevent_overwrites | Bool | True |
| only_cache | Bool | False |
| dataloader_threads | Float32 | 8.0 |
| train_device | String | "cuda" |
| temp_device | String | "cpu" |
| multi_gpu | Bool | False |
| device_indexes | String | "0" |
| fused_gradient_reduce | Bool | False |
| async_gradient_reduce | Bool | False |

#### Model tab
| field | type | default |
|---|---|---|
| base_model_name | String | SERENITY_KLEIN9B_CHECKPOINT |
| vae_override | String | "" (read-only field except krea2 preset sets it) |
| output_model_destination | String | SERENITY_TRAINER_OUTPUT_DIR |
| output_model_format | String | "SAFETENSORS" |
| output_dtype | String | "FLOAT_16" |
| model_arch | String | "klein9b" (preset-derived) |
| model_quantize | Bool | True (snapshot-only) |
| model_quantize_text_encoder | Bool | True (snapshot-only) |
| model_low_vram | Bool | True (snapshot-only) |
| model_qtype_text_encoder | String | "qfloat8" (snapshot-only) |
| train_transformer | Bool | True |
| train_text_encoder | Bool | False |
| gradient_checkpointing | Bool | True |
| activation_offloading | Bool | False |
| layer_offload_fraction | Float32 | 0.0 |

#### Dataset / Concepts
| field | type | default |
|---|---|---|
| dataset_path | String | SERENITY_BOXJANA_DATASET_DIR |
| sample_output_dir | String | SERENITY_TRAINER_OUTPUT_DIR |
| concept_file_name | String | "concepts.json" |
| concepts | List[TrainerUIConcept] | one "boxjana" concept (path, trigger "box1jana", 22 imgs, repeats 1, STANDARD, enabled) |
| aspect_ratio_bucketing | Bool | True ([not wired]) |
| latent_caching | Bool | True |
| cache_text_embeddings | Bool | True |
| clear_cache_before_training | Bool | False |
| resolution | String | "512" |
| caption_extension | String | "txt" |
| caption_dropout | Float32 | 0.0 |

`TrainerUIConcept` (TrainerConfigModel.mojo:79): name, path, trigger,
image_count Int32, repeats Int32, concept_type String ("STANDARD"), enabled Bool.

#### Training tab — schedule / optimizer / precision
learning_rate 0.0004, text_encoder_learning_rate 1e-5, transformer_learning_rate
0.0004, epochs 1.0, max_train_steps 3000.0, batch_size 1.0,
gradient_accumulation_steps 1.0, learning_rate_warmup_steps 0.0,
learning_rate_min_factor 0.0, learning_rate_cycles 1.0, learning_rate_scaler
"NONE", weight_decay 0.01, train_dtype "BFLOAT_16", fallback_train_dtype
"BFLOAT_16", ema_mode "OFF", ema_decay 0.999, ema_update_step_interval 5.0,
enable_autocast_cache True, frames "25", force_circular_padding False, seed 42.0,
clip_grad_norm 1.0, text_encoder_stop_after 30.0, text_encoder_sequence_length
"512", transformer_stop_after 0.0, transformer_attention_mask False,
transformer_guidance_scale 1.0 (TrainerConfigModel.mojo:588-614).

#### Training tab — loss / noise / masking
loss_weight_strength 5.0, mse_strength 1.0, mae_strength 0.0, log_cosh_strength
0.0, huber_strength 0.0, huber_delta 1.0, vb_loss_strength 0.0, loss_weight_fn
"MIN_SNR_GAMMA", **loss_fn "mse"** (T1.A lever), smooth_l1_beta 1.0,
**min_snr_gamma_flow 0.0** (T1.A lever), **quantized_resident "OFF"** (T2.B lever;
no dropdown — fixed default, preset can set "fp8_e4m3"), loss_scaler "NONE",
offset_noise_weight 0.0, perturbation_noise_weight 0.0, timestep_distribution
"UNIFORM", timestep_type "linear", noise_scheduler "flowmatch",
min_noising_strength 0.0, max_noising_strength 1.0, noising_weight 1.0,
noising_bias 0.0, timestep_shift 1.0, dynamic_timestep_shifting False,
masked_training False ([not wired]), unmasked_probability 0.0, unmasked_weight
0.0, normalize_masked_area_loss False, masked_prior_preservation_weight 1.0,
custom_conditioning_image False, layer_filter_preset "full", layer_filter "",
layer_filter_regex False (TrainerConfigModel.mojo:615-647).

#### LoRA / OFT tab
peft_type "LORA", lora_model_name "boxjana_klein9b_lora_v1", lora_rank 16.0,
lora_alpha 16.0, lora_dropout 0.0, lora_weight_dtype "FLOAT_32",
bundle_additional_embeddings True, oft_block_size 4.0, oft_coft False
(TrainerConfigModel.mojo:649-657).

#### Sampling tab
samples List[TrainerUISample] (two default klein prompts,
TrainerConfigModel.mojo:659-661), sample_after 500.0, sample_skip_first 0.0,
sample_cfg 7.0, sample_steps 20.0, sample_sampler "FlowMatch Euler",
sampler_preset "KLEIN_20", samples_to_tensorboard True, non_ema_sampling False.
`TrainerUISample` (TrainerConfigModel.mojo:116): prompt, negative_prompt, seed
Int32 (default 42).

#### Backup tab
backup_after 500.0, rolling_backup True, rolling_backup_count 5.0,
backup_before_save True, save_every 500.0, save_skip_first 0.0, save_max_keep
4.0, save_filename_prefix "boxjana_klein9b_lora_v1"
(TrainerConfigModel.mojo:671-678).

#### Cloud tab
cloud_host "", cloud_port "22", cloud_user "root", cloud_workspace_dir
"/workspace/serenity", cloud_delete_workspace False (TrainerConfigModel.mojo:680-684).

### The `architecture_options` list (all 13, index-stable ordering rule)

Defined at TrainerConfigModel.mojo:372-388. **The ordering rule (stated in the
source comments) is: new architectures are appended LAST so existing option
indices stay stable** — HiDream-O1 and Krea-2 were appended last for exactly this
reason (TrainerConfigModel.mojo:384-387; model_type equivalents 365-368).

| idx | architecture_options label | model_type_options label | backend_target |
|---|---|---|---|
| 0 | Ideogram4 FP8 | IDEOGRAM_4 | ideogram4 |
| 1 | Klein 9B | FLUX_2 | klein |
| 2 | Flux2 Dev | FLUX_2 | klein (routes to Klein 9B default) |
| 3 | SDXL 1.0 | STABLE_DIFFUSION_XL_10_BASE | sdxl |
| 4 | Chroma1 HD | CHROMA_1 | chroma |
| 5 | Ernie Image | ERNIE_IMAGE | ernie |
| 6 | Anima | ANIMA | anima |
| 7 | Z-Image | Z_IMAGE | zimage |
| 8 | Z-Image L2P | Z_IMAGE_L2P | l2p |
| 9 | LTX-2 AV | LTX_2_VIDEO | ltx2 (unwired — fail-loud) |
| 10 | Wan2.2 T2V 14B | WAN_22_VIDEO | wan22 (unwired — fail-loud) |
| 11 | HiDream O1 | HIDREAM_O1 | hidream |
| 12 | Krea 2 Raw | KREA_2 | krea2 |

Note: `model_type_options` also contains `STABLE_DIFFUSION_35` at model-type
index 3 (TrainerConfigModel.mojo:357) which has **no** architecture entry — it
routes to backend "sd35" and fails loud (see §8).

### `_model_type_for_arch_index` / `_arch_index_for_model_type`

The two selectors (Model Type, Architecture) are kept in sync by two mapping
functions:
- `_arch_index_for_model_type(mt) -> arch` (TrainerConfigModel.mojo:760): maps a
  model-type index to the canonical architecture index. Model-type 3
  (STABLE_DIFFUSION_35) returns -1 → "no trainable runner yet".
- `_model_type_for_arch_index(arch) -> mt` (TrainerConfigModel.mojo:789): the
  inverse; arch 1 AND arch 2 (Klein 9B / Flux2 Dev) both map to model-type 1
  (FLUX_2).

`trainer_ui_apply_model_preset(cfg, prefer_model_type)` (TrainerConfigModel.mojo:818)
resolves which selector the user changed: if `prefer_model_type` it derives arch
from `_arch_index_for_model_type(model_type_index)` (arch<0 → sd35 fail-loud
route), else it derives model_type from arch. ModelTab passes
`model_changed` as `prefer_model_type` (ModelTab.mojo:62).

### `trainer_ui_apply_model_preset` — the FULL per-architecture preset table

The single most important artifact for the web UI. When a model/architecture
selector changes, this sets backend_target, checkpoint/cache/workspace paths,
lr/rank/alpha, sampler preset, quantized_resident, cadences. Each branch also
pins both selector indices. Source: TrainerConfigModel.mojo:835-1021. **Only
fields listed per branch are set; everything else keeps its current value** (this
is a real bug — see §8 cross-preset contamination).

**arch 1 or 2 — Klein 9B / Flux2 Dev → backend "klein"** (835):
model_type_index=1, architecture_index=1, base_model_name=SERENITY_KLEIN9B_CHECKPOINT,
cache_dir=SERENITY_BOXJANA_KLEIN_CACHE, model_arch="klein9b",
sample_sampler="FlowMatch Euler", sampler_preset="KLEIN_20". (Does NOT set
lr/rank/alpha/run_name/workspace — keeps struct defaults or prior values.)

**arch 0 — Ideogram4 FP8 → "ideogram4"** (844): base=SERENITY_IDEOGRAM4_BASE,
cache=SERENITY_IDEOGRAM4_CACHE, model_arch="ideogram4",
sample_sampler="Ideogram4 FlowMatch", sampler_preset="V4_DEFAULT_20", and
REPLACES samples with one gigerver3 JSON-caption sample (854).

**arch 3 — SDXL 1.0 → "sdxl"** (855): base=SERENITY_SDXL_CHECKPOINT,
cache=SERENITY_SDXL_CACHE, model_arch="sdxl10", sampler="Euler",
sampler_preset="SDXL_20", **lr=0.0001, lora_rank=16, lora_alpha=16**,
timestep_shift=1.0. (Trainer compiled for rank 16 — fails loud otherwise.)

**arch 4 — Chroma1 HD → "chroma"** (871): base=SERENITY_CHROMA_CHECKPOINT,
cache=SERENITY_CHROMA_CACHE, model_arch="chroma1hd", sampler="FlowMatch Euler",
sampler_preset="CHROMA_20", **lr=0.0001, rank=16, alpha=16**, timestep_shift=1.15.

**arch 5 — Ernie Image → "ernie"** (887): base=SERENITY_ERNIE_CHECKPOINT,
cache=SERENITY_ERNIE_CACHE, model_arch="ernie_image", sampler="FlowMatch Euler",
sampler_preset="ERNIE_20", **lr=0.0003, lora_alpha=1.0** (NOT rank),
timestep_shift=1.0.

**arch 6 — Anima → "anima"** (902): base=SERENITY_ANIMA_CHECKPOINT,
cache=SERENITY_ANIMA_CACHE, model_arch="anima", sampler="FlowMatch Euler",
sampler_preset="ANIMA_20", **lr=0.0001, lora_alpha=16**, timestep_shift=1.0.

**arch 7 — Z-Image → "zimage"** (916): base=SERENITY_ZIMAGE_CHECKPOINT,
cache=SERENITY_ZIMAGE_CACHE, model_arch="zimage", sampler="FlowMatch Euler",
sampler_preset="ZIMAGE_20", **lr=0.0003, rank=16, alpha=1.0**, timestep_shift=1.0.
(NOTE: SERENITY_ZIMAGE_CACHE is MISSING on disk — see §8.)

**arch 8 — Z-Image L2P → "l2p"** (932): base=SERENITY_L2P_CHECKPOINT,
cache=SERENITY_L2P_CACHE, model_arch="zimage_l2p", sampler="FlowMatch Euler",
sampler_preset="L2P_20", **lr=0.0003, rank=16, alpha=16**, timestep_shift=3.0.
(No prepared L2P pixel cache exists — trainer preflight fails loud.)

**arch 9 — LTX-2 AV → "ltx2"** (950): base="", model_arch="ltx2_av",
frames="25". **Unwired backend — launch fails loud** (readiness contract).

**arch 10 — Wan2.2 T2V 14B → "wan22"** (961):
base="/home/alex/.serenity/models/checkpoints/wan2.2_t2v_low_noise_14b_fp16.safetensors",
model_arch="wan22_t2v_14b", frames="1". **Unwired — fail loud** (placeholder RoPE).

**arch 11 — HiDream O1 → "hidream"** (972): base=SERENITY_HIDREAM_CHECKPOINT,
cache=SERENITY_HIDREAM_STAGE, model_arch="hidream_o1", sampler="FlowMatch Euler",
sampler_preset="HIDREAM_20", **lr=0.0001, rank=32, alpha=32**, timestep_shift=3.0.

**arch 12 — Krea 2 Raw → "krea2"** (989) — the fullest preset:
run_name="eri2_krea2_lora", base=SERENITY_KREA2_CHECKPOINT,
vae_override=SERENITY_KREA2_VAE, cache=SERENITY_KREA2_CACHE,
workspace_dir=SERENITY_KREA2_WORKSPACE, sample_output_dir=workspace+"/samples",
model_arch="krea2_raw", sampler="FlowMatch Euler", sampler_preset="KREA2_20",
**lr=0.0001, rank=64, alpha=64**, peft_type="LORA", timestep_shift=1.0,
**quantized_resident="fp8_e4m3"**, sample_cfg=3.5, sample_steps=30,
sample_after=500, max_train_steps=2000, save_every=500,
save_filename_prefix="eri2_krea2", frames="1", resolution="512",
text_encoder_sequence_length="384". **Krea-2 is the ONLY preset that pins
run_name / workspace_dir / sample_output_dir / save_filename_prefix.**

### The comptime path constants (per model)

All at TrainerConfigModel.mojo:18-76. These are the checkpoint/cache/VAE/
sample-prompt file locations the presets and JSON emitters reference. Key ones:
SERENITY_TRAINER_OUTPUT_DIR="/home/alex/mojodiffusion/output",
SERENITY_KLEIN9B_CHECKPOINT, SERENITY_KLEIN_VAE, SERENITY_KLEIN_SAMPLE_PROMPTS,
SERENITY_IDEOGRAM4_BASE/_CACHE, SERENITY_KREA2_CHECKPOINT/_VAE/_CACHE/_WORKSPACE,
SERENITY_HIDREAM_CHECKPOINT/_STAGE, SERENITY_CHROMA/_ERNIE/_ANIMA/_SDXL/_ZIMAGE/
_L2P checkpoint+cache+vae, and per-model *_SAMPLE_PROMPTS JSONs under
`/home/alex/mojodiffusion/serenitymojo/configs/`. Several carry provenance
comments noting stale/missing paths (e.g. ZIMAGE_CACHE MISSING at line 65,
L2P_CACHE was alina at 69, IDEOGRAM4_CACHE "was ideogram4_giger_cache" at 29).

### Config snapshot persistence

Two functions, both anchored to a single fixed file
`target/serenity_trainer_ui_config.json` (relative to the serenity-trainer cwd):

- **Save** — `trainer_ui_config_json_snapshot(cfg) -> String`
  (TrainerConfigModel.mojo:1557) emits the FULL config as one flat JSON object
  with `"schema":"serenity.trainer_ui.v1"` and ~140 keys (every scalar + label
  strings for dropdowns). Written by `_save_config_snapshot`
  (TrainerRuntimeBridge.mojo:878) on Start (submit) and on Save-checkpoint. All
  string values pass through `trainer_ui_json_escape`; all Bools through
  `trainer_ui_json_bool` (lowercase true/false — fixes the Python-style
  True/False bug, TrainerConfigModel.mojo:1457-1461).
- **Load** — `trainer_ui_load_config_snapshot(mut cfg) -> Bool`
  (TrainerConfigModel.mojo:1497) reads the same file at UI init (TrainUI.mojo:108)
  and restores ONLY a CURATED subset: max_train_steps, learning_rate, lora_rank,
  lora_alpha, save_every, sample_after, sample_steps, sample_cfg, seed, epochs,
  caption_dropout, run_name. **Paths and model selection are NOT restored — they
  stay preset-driven.** Fail-soft: any parse error leaves cfg untouched. This
  exists because otherwise max_train_steps reset to the struct default 3000 every
  launch (the "3000 steps hardcoded" report).

---

## 4. LAUNCH CONTRACT

### What Start does end-to-end

Start button (TopBar.mojo:62-63) calls `trainer_ui_submit_current(cfg, rt)`
(TrainerRuntimeBridge.mojo:755):
1. Sets `rt.backend_target`/`backend_label` from cfg (756-757).
2. Guards against an already-running run (758-761).
3. `trainer_ui_validate(cfg)` — must return "Ready" or it aborts
   (762-765). Validation checks: base model present, dataset path present,
   lora_rank≥1, lr>0, and LTX2-only LyCORIS block (TrainerConfigModel.mojo:1033).
4. Capability-table honesty: `trainer_ui_ignored_lever_summary(cfg)` — logs a
   WARNING naming every non-default widget the runner does not consume, but does
   NOT block launch (769-772).
5. `_save_config_snapshot(cfg)` → writes `target/serenity_trainer_ui_config.json`
   (775, 878).
6. Bumps run_id, resets live stats, records progress-file byte offset baseline,
   refreshes system metrics (781-810).
7. Appends a `start` event to the command file
   `target/serenity_trainer_commands.jsonl` (814, `_append_command_event`).
8. `trainer_ui_launch_live_runner(cfg, rt)` (816) — the actual spawn.
9. On success sets `rt.has_running=True` and appends a run to `rt.runs` (820-821).

### Config JSON emission (per model)

`trainer_ui_launch_live_runner` (TrainerRuntimeBridge.mojo:544):
- Rejects backends with no wired runner (545-556): only ideogram4, the
  config-runner set, hidream, and krea2 are allowed. Anything else (sd35, ltx2,
  wan22) → "No trainer wired for <backend>", returns False.
- `needs_runner_config` (570) is true for all config-runners + hidream + krea2,
  and for ideogram4 ONLY when a lever is set. When true, writes the runner train
  config JSON via `_write_runner_train_config` (536) to
  **`target/serenity_<backend>_train_config.json`** (`_runner_train_config_path`,
  line 279).
- The JSON body is `trainer_ui_runner_train_config_json(cfg)`
  (TrainerConfigModel.mojo:1195). It branches per backend and emits: model_type,
  checkpoint, vae, validation_prompts_file, the full architecture dims (verbatim
  from `serenitymojo/configs/<model>.json` — the runner re-validates every dim
  and fails loud on drift), the T1 lever keys (loss_fn/huber_delta/smooth_l1_beta/
  min_snr_gamma_flow/optimizer/optimizer_warmup_steps), and the shared recipe tail
  `_runner_recipe_json` (learning_rate, network_algorithm, adapter_algo, lora_rank,
  lora_alpha, timestep_shift, caption_dropout_prob, ema/ema_decay/ema_update, 
  max_grad_norm, max_steps, save_every, sample_every, seed, frames, cache_dir,
  optimizer eps/wd/betas — TrainerConfigModel.mojo:1159-1192). Backends covered:
  klein, krea2, chroma, ernie, anima, zimage, l2p, sdxl, hidream, ideogram4;
  anything else raises "no runner train config template" (1421).
- Ideogram4 additionally writes a sample-prompt file
  `target/serenity_ideogram4_sample_prompt.json` when sample_after>0
  (`_write_ideogram4_sample_prompt`, line 287) — it writes the FIRST sample's
  prompt text raw.

### argv construction per backend (the exact argv shapes)

`_live_runner_command(cfg, rt)` (TrainerRuntimeBridge.mojo:297) builds the args
string. There are **4 distinct argv shapes**:

1. **krea2** (`_is_krea2_target`, 315): `<cache.safetensors> <steps>
   <train_config.json>` — the cache is positional (primary dataset artifact).
2. **config runners** (klein, chroma, ernie, anima, sdxl, zimage, l2p —
   `_is_config_runner_target`, 197/326): `<train_config.json> <steps>`.
3. **hidream** (`_is_hidream_target`, 335): `<stage_dir> <steps> <lr> <rank>
   <out_dir> - <config.json>` — argv wins for steps/lr/rank/out_dir; ema_decay
   "-" so config owns EMA; trailing config.json delivers the levers. out_dir =
   `<OUTPUT_DIR>/hidream_o1_lora`.
4. **ideogram4** (else branch, 354): a long positional line —
   `<progress_file> <transformer.safetensors> <cache_dir> <OUTPUT_DIR> <steps>
   <rank> <alpha> <lr> <save_every> <caption_dropout> <levers_json|-> <sample_after>
   <sample_steps> <sample_cfg> <seed> - <sample_prompt.json|-> <resolution>`. The
   levers path is "-" (skip sentinel) unless a lever is non-default
   (`trainer_ui_ideogram4_levers_path_or_skip`, TrainerConfigModel.mojo:1445).
   The transformer path is `<base_model>/transformer/diffusion_pytorch_model.safetensors`
   (line 190).

Each backend also prepends `mkdir -p` self-heal for its output dirs
(TrainerRuntimeBridge.mojo:404-440): hidream mkdirs the hidream_o1_lora dir;
krea2 mkdirs workspace + workspace/samples; config runners mkdir a fixed set —
**klein uses the stale `alina_train` dir** (line 429), others mkdir
chroma_boxjana/alina_sdxl/alina_zimage/alina_l2p/etc (433-439).

The full launch command (441-460) is:
```
cd trainer && <mkdirs> rm -f <pidfile> <termpidfile> &&
  bash scripts/serenity_terminal_launcher.sh <backend_label> <runner_path>
  <pidfile> <logfile> <args>
```
Runner path = `target/serenity_<backend>_live_trainer` for config/hidream/krea2
(`_runner_path`, 258), or `target/serenity_ideogram4_live_trainer` (const
`IDEOGRAM4_LIVE_RUNNER`, 24). The whole thing runs via a libc `system()` FFI call
(`_sys_system`, 153).

### serenity_terminal_run.sh's role

The launcher `scripts/serenity_terminal_launcher.sh` (invoked above) does two
things: (a) **heredoc-writes** `target/serenity_terminal_run.sh` fresh each launch
(so that file is a generated artifact, not source), then (b) `env -i` scrubs the
environment and re-exports a fixed GNOME + CUDA env (HOME, USER, DISPLAY=:1,
XDG_RUNTIME_DIR=/run/user/1000, DBUS_SESSION_BUS_ADDRESS, PATH with CUDA,
LD_LIBRARY_PATH with CUDA libs) and launches `/usr/bin/gnome-terminal
--title="Serenity <label> Trainer" -- bash <terminal_script> <root> <pidfile>
<logfile> <label> <runner> <args>`.

`target/serenity_terminal_run.sh` itself (the generated wrapper): cd's to root,
writes its OWN pid to `<pidfile>.term` (the terminal-wrapper pid), prints a
banner, runs `"$runner" "$@" > >(tee "$logfile") 2>&1 &`, writes the child
runner pid to `<pidfile>`, installs a `trap stop_child TERM INT HUP` that
TERMs the child + cleans pidfiles + exits 143, `wait`s on the child, and on
nonzero exit shows a 30s "closing" prompt (success/stopped closes immediately).

Because `gnome-terminal` returns immediately (spawn goes over D-Bus to
gnome-terminal-server), the launcher polls for up to 20s for `<pidfile>` or
`<logfile>` to appear, and returns nonzero if the runner never started ("training
started but no terminal opened" guard).

**pid files** (per backend, `_pid_path`/`_term_pid_path`, 264/484):
`target/serenity_<backend>_live_trainer.pid` (runner pid) and `.pid.term`
(terminal-wrapper pid). ideogram4 uses `target/serenity_ideogram4_live_trainer.pid`.

**progress log** (`_log_path`, 270): config/hidream/krea2 runners tee stdout
straight into `target/serenity_trainer_progress.log` (the polled progress file);
ideogram4 tees into `/tmp/serenity_ideogram4_live_trainer.log`.

There is also an older `target/serenity_ideogram4_terminal_run.sh` and
`scripts/serenity_ideogram4_terminal_launcher.sh` — a legacy ideogram4-specific
variant (keeps a "Press Enter to close" hold). The current code path uses the
generic launcher.

### Process lifecycle (stop / kill)

- **Stop** (TopBar.mojo:59) → `trainer_ui_cancel` (TrainerRuntimeBridge.mojo:848):
  if an owned runner is active, `_stop_live_runner` (494) TERMs the terminal
  WRAPPER pid first (its trap tears down the runner AND closes the window), falls
  back to TERMing the runner pid, and removes the pidfiles only when a kill
  landed. Sets rt to Stopped, appends a `stop` command event.
- **Liveness guards**: `_owned_live_runner_active` (468) requires the pidfile pid
  to be alive AND its `/proc/<pid>/cmdline` to actually be this runner (defends
  against recycled pids from stale pidfiles). `_any_live_runner_active` (463)
  `pgrep`s for any serenity trainer.
- **Auto-detect exit**: `trainer_ui_tick_and_apply` (1212), ~once/sec after a 3s
  grace, checks `_owned_live_runner_active`; if the process vanished it calls
  `_record_live_runner_exit` (516) — re-reads the log for a final progress line,
  removes pidfiles, sets "Trainer process exited".

### Resume argv (the MJ-1077 trap)

**UNVERIFIED / GAP:** There is NO resume-with-state argv in the UI code. The
ideogram4 argv has an "argv 16 = optional resume LoRA path" slot but the UI
always passes `-` (skip sentinel) for it (TrainerRuntimeBridge.mojo:399 emits a
literal `-`). `continue_last_backup` is a General-tab toggle that is snapshot-only
(never reaches any argv). So the known MJ-1077 trap — full resume needs the
`.state` path — is **not implemented**: the UI cannot resume a run from optimizer
state; a relaunch starts fresh. A web UI must add this if resume is required.

---

## 5. PROGRESS + STATUS

### Progress log tailing / parsing

The pump is `trainer_ui_tick_and_apply` (TrainerRuntimeBridge.mojo:1212), called
every frame from `_frame`:
- Every 60 frames refreshes system metrics (1214-1216).
- If running and not paused, every 6 frames (~100 ms) calls
  `trainer_ui_poll_progress_file` (1224). That reads the WHOLE progress file,
  applies only bytes past the last offset (`rt.progress_file_bytes`), splits on
  `\n`, and feeds each line to two parsers (`_apply_progress_text_from`, 1167).
- The file re-read is intentionally throttled off the 60 Hz render thread (the
  file is tee'd full trainer stdout, grows monotonically).

Two accepted line shapes:
1. **Serenity callback** (`trainer_ui_apply_serenity_callback_line`, 1004):
   `[Serenity-callback] progress epoch 1/10 | step 44/120 | global_step 164 |
   loss 0.5909 | smooth_loss 0.6123 | grad_norm 0.1527 | lr 0.000100 | status
   Training`. Extracted fields: epoch/total_epoch, step/total_step, global_step,
   loss, smooth_loss, grad_norm, lr, status.
2. **Klein/tqdm progress** (`trainer_ui_apply_progress_line`, 1067):
   `[Klein-lora] step 1613/2000 | epoch 14/17 | loss 0.5909 | grad_norm 0.1527 |
   2.1s/step | elapsed 0:55:37 | ETA 0:13:20`. Extracted: step/total_steps,
   epoch/total_epochs, loss, grad_norm, optional smooth_loss (falls back to
   UI-side EMA), s/step speed, ETA seconds. Global_step is derived as
   `(epoch-1)*total_steps + step`.

Parsing is done entirely on RAW BYTES (`_find_token`, `_find_char`, `_token_end`,
`_read_int_between`, `_read_float_between`, `_parse_seconds`) because trainer
stdout can contain multi-byte UTF-8 and String codepoint indexing at an arbitrary
byte offset asserts (TrainerRuntimeBridge.mojo:905-908 documents this). Both
parsers parse into locals FIRST and commit only after the whole line validates —
this is the fix for inline-sampler lines corrupting the step counter (bug 1,
audit 2026-07-03).

Completion is `_live_progress_complete` (723): total reached, or step≥total and
epoch≥total_epochs. ETA is recomputed by `_update_eta_like_serenity` (637) from
wall time / steps-done × remaining-steps (1-based epoch clamp).

### The status rail (`_status_rail`, TrainUI.mojo:398)

Fields shown: RUNNING/PAUSED/IDLE pill, a progress bar
(`trainer_ui_progress_fraction`), then Step, Epoch, Loss, Smooth, Grad, LR, Speed
(s/step), ETA (`trainer_ui_eta_label`), Status, Command, Source
(callbacks/progress-file/waiting). Artifacts block: Samples count, Checkpoints
count, Backend. Hardware block: GPU Model, Driver, GPU util%+temp, VRAM used/total,
CPU Model, CPU util%, RAM used/total.

GPU/CPU stats source: `trainer_ui_refresh_system_metrics`
(TrainerRuntimeBridge.mojo:739) calls `Backend.system_metrics()` (a MojoUI
backend API) → gpu_name, gpu_driver, gpu_util_percent, gpu_temperature_c,
gpu_memory_used/total_mb, cpu_name, cpu_util_percent, ram_used/total_mb. The web
UI must replace this with a server-side probe (nvidia-smi / /proc).

### Run history

`rt.runs: List[String]` — appended in `trainer_ui_submit_current`
(TrainerRuntimeBridge.mojo:821) as `"#<id> <run_name> - <backend> target"`.
Displayed on the Runs tab (`_runs_tab`, TrainUI.mojo:242). It is in-memory only —
NOT persisted across UI restarts. `rt.logs: List[String]` (capped 512,
TrainerRuntimeBridge.mojo:128) is the event log shown on the Logs tab (last 10).

---

## 6. MEDIA GALLERY

### Sample / dataset discovery

`load_preview_textures` (TrainerMediaGallery.mojo:465) is called every frame from
`_frame`. It keys on `dataset_path + "\n" + sample_output_dir`; on first sight it
`_load_scanned_gallery` (273) which scans both dirs via
`Backend.scan_media_files(dir, recursive=True, cap=4096)` (217/245). Dataset
tiles preload the first 48 textures, sample tiles the first 32; the rest load
lazily on scroll. Titles are "Dataset N"/"Sample N"; subtitles use `_basename` of
the file path (image/video prefix for samples).

While training, a throttled (~3 s / 180 frames) count-only rescan
(`_refresh_sample_gallery`, 430) rebuilds the sample gallery iff the file count
changed — this is how new inline samples appear during a run. It is skipped while
a lightbox preview is open (so the active texture is not destroyed).

### Texture load pipeline

- `_load_media_texture` (170) → `Backend.load_video_thumbnail_info` for videos
  else `Backend.load_texture_file_info(path, max_w, max_h)`.
- Virtual scrolling: `ensure_visible_dataset_textures`/`_sample_` (354/382) plus
  `_ensure_media_range` (309) compute the visible tile window from scroll_y and
  gallery geometry (`gallery_columns`, `gallery_card_height`, `gallery_visible_rows`,
  279-306), load textures in `[start,end)`, and destroy textures outside a
  ±96-tile keep band to bound VRAM. The active lightbox texture is never destroyed.
- Grid render uses the shared MojoUI `media_virtual_grid` widget (DatasetTab.mojo:93,
  SamplingTab.mojo:81, CaptionerTab.mojo:392). Column count = content_w//360
  clamped [2,8]; card ~4:3.

### Preview interaction (lightbox)

Clicking a tile returns its index from the grid widget; `_main_section` calls
`open_dataset_preview`/`open_sample_preview` (TrainerMediaGallery.mojo:535/560)
which lazily upgrade the tile to a 960² texture and set the `active_preview_*`
state. `render_media_lightbox` (585) draws the overlay via the MojoUI
`media_lightbox` widget; actions: `MEDIA_LIGHTBOX_PLAY` → `Backend.open_video_file`
(external player) for videos, `MEDIA_LIGHTBOX_CLOSE` → clears active preview.

### The UTF-8 `_basename` fix (commit 2c81bbb)

`_basename` (TrainerMediaGallery.mojo:156) scans RAW BYTES for `/` (0x2F) and
slices at `last+1`, which is always a codepoint boundary because `/` is
single-byte ASCII. The comment explicitly notes that the naive
`String(path[byte=i])` per-char form asserts when byte i lands mid-codepoint —
i.e. it crashed on any non-ASCII path char. This is the gallery's version of the
crash class that still lives in CaptionerTab (see §8).

### Video handling

Video thumbnails are detected two ways: `Backend.is_video_file(path)` and a
thumbnail-path convention `<name>.<ext>-...` (`_video_thumbnail_source_end`, 87,
recognizing .mp4/.mov/.m4v/.mkv/.avi/.webm). `_video_source_path` (113) strips the
thumbnail suffix back to the source video for playback. Sample/dataset subtitles
tag "video" vs "image".

`rt.samples` (status rail count) is fed from the scanned sample gallery paths via
`_sync_runtime_samples` (422) — it was never populated elsewhere.

---

## 7. OTHER TABS

- **Dataset** (DatasetTab.mojo): editable dataset_path + resolution select +
  latent-caching/clear-cache toggles + caption_dropout slider; the rest
  (concept file, buckets, caption source, cache format) are read-only field rows;
  a virtual media grid. "Aspect Buckets [not wired]" toggle is snapshot-only
  (DatasetTab.mojo:53). Preview/caption summary is derived from scanned textures.
- **Captioner** (CaptionerTab.mojo): the most functional aux tab. Selects a
  Qwen-VL caption model (custom-model edit_row when "Custom...", CaptionerTab.mojo:321),
  quant/attention/resolution, folder path, prompt, toggles, max-tokens slider.
  Buttons: Load/Unload model, Scan Folder (in-process scan via
  `Backend.scan_media_files`), Queue Caption Run / Abort. Actions append to
  **`target/serenity_captioner_commands.jsonl`** (`_append_captioner_command`, 242)
  for a separate pure-Mojo runner to consume — no runner is wired in this repo
  (UNVERIFIED that a consumer exists). Has its own media grid + current-media
  sidecar preview. **Its `_basename`/`_sidecar_path`/`_json_escape` use the
  crash-class single-byte index at CaptionerTab.mojo:68, 79, 111 — same class the
  gallery fixed (see §8).**
- **Sampling** (SamplingTab.mojo): editable sample_output_dir + cadence drags
  (sample_after/skip/steps/cfg) + sampler select + tensorboard/non-ema toggles;
  the sample-prompt list is READ-ONLY field rows (prompts are set only by presets,
  not editable in the UI); a sample media grid.
- **Backup** (`_backup_tab`, TrainUI.mojo:214): a BACKUP panel of READ-ONLY field
  rows (backup_after, rolling_backup, rolling_count, before_save, save_every,
  save_prefix) and a **PRESET SNAPSHOT** panel showing schema, backend,
  architecture label, validation status, and the config file path
  `target/serenity_trainer_ui_config.json`. There is no snapshot save/load button
  here — persistence is automatic on Start/Save (see §3).
- **Runs** (`_runs_tab`, TrainUI.mojo:242): read-only list of `rt.runs`.
- **Logs** (`_logs_tab`, TrainUI.mojo:254): OUTPUT EVENTS (last 10 of `rt.logs`)
  + ARTIFACTS panel (progress/command file paths, stats source, sample/checkpoint
  counts and last few paths).
- **Cloud** (CloudTab.mojo): editable cloud-type select + delete toggle; host/
  port/user/remote-dir are READ-ONLY field rows; SYNC panel is entirely
  placeholder text. Cloud sync is NOT implemented — surface only.
- **Validations/Concepts** (ConceptTab.mojo): concept list + detail, mostly
  read-only; only the first concept's `enabled` toggle is interactive.

---

## 8. KNOWN DEBT

1. **Cross-preset contamination (run_name/workspace/prefix leak).** Only the
   Krea-2 preset sets run_name / workspace_dir / sample_output_dir /
   save_filename_prefix (TrainerConfigModel.mojo:995-1018). No other preset
   resets them. Selecting Krea-2 then switching to Klein (or any other model)
   leaves `run_name="eri2_krea2_lora"`, `workspace_dir=SERENITY_KREA2_WORKSPACE`,
   `save_filename_prefix="eri2_krea2"` in place — the Klein run then displays and
   snapshots Krea-2's naming. The web UI should reset these per preset.

2. **Klein stale `alina` naming + UI/trainer output-dir mismatch.** The klein
   launch mkdir hardcodes `/home/alex/mojodiffusion/output/alina_train`
   (TrainerRuntimeBridge.mojo:429) and the comment states `train_klein_real`
   saves LoRAs/samples into its comptime `LORA_DIR`/`SAMPLE_DIR` (alina_train).
   Meanwhile the Klein preset never sets workspace_dir, so the UI shows the struct
   default `/home/alex/trainings/boxjana_klein9b_lora_v1` (TrainerConfigModel.mojo:538)
   — the displayed workspace does not match where klein actually writes. The klein
   cache constant also carries a "was alina_klein9b" provenance note
   (TrainerConfigModel.mojo:20).

3. **Empty caps-cache path crash (`cap_cache.header open failed`).**
   **UNVERIFIED from UI code:** this is a downstream `train_klein_real` behavior —
   when the klein cache dir is empty/missing the trainer fails opening the caption
   cache header. The UI does not preflight the cache; it will happily launch a
   klein run against a missing `SERENITY_BOXJANA_KLEIN_CACHE`. A web UI should
   validate cache existence before launch. Related: `SERENITY_ZIMAGE_CACHE` is
   annotated MISSING (TrainerConfigModel.mojo:65) and `SERENITY_L2P_CACHE` has no
   prepared cache (line 67-69) — those presets launch straight into a fail-loud
   trainer preflight.

4. **Remaining byte-indexed String slicing crash sites.** Grep of `ui/` for the
   single-byte `String(x[byte=i])` form (asserts on multi-byte UTF-8) finds THREE
   live sites, all in **CaptionerTab.mojo**: `_basename` line 68, `_sidecar_path`
   line 79, `_json_escape` line 111. These crash the captioner on any non-ASCII
   path or caption byte — the same class the media gallery already fixed with a
   raw-byte scan (TrainerMediaGallery.mojo:156-167) and the progress bridge avoids
   (TrainerRuntimeBridge.mojo:905-908). (Range slices `[byte=a:b]` at known
   boundaries are safe and used widely; the dangerous form is the single-index
   `[byte=i]` codepoint access.) The web UI, being string-native, eliminates this
   class entirely.

5. **[not wired] widgets that must not silently lie.** Several widgets are
   snapshot-only — no runner consumes them: Aspect Buckets (DatasetTab.mojo:53),
   the entire MASKED TRAINING panel (TrainingTab.mojo:158-165), Train Method
   Fine-Tune/Embedding (ModelTab.mojo:58), Pause/Resume/Sample-now (TopBar.mojo:82-90,
   the command file is written but no trainer reads it). The capability table
   (`trainer_ui_supported_lever_keys` / `_active_lever_keys` /
   `_ignored_lever_summary`, TrainerConfigModel.mojo:1080-1156) drives a
   "<backend> ignores: …" WARNING in the top bar and submit log so non-consumed
   non-default widgets are named loudly rather than silently dropped. Per-backend
   consumed levers: klein/zimage/hidream/ideogram4 = optimizer, warmup, loss_fn,
   min_snr_gamma_flow, ema, caption_dropout, network_algorithm; krea2 = those
   minus ema/caption_dropout; chroma/ernie/anima/sdxl/l2p/qwen = network_algorithm
   only.

6. **Hardcoded values that violate the config-driven rule.** Beyond the mkdir dirs
   in §2: several tab panels render fixed placeholder strings instead of live
   config (e.g. Dataset "Buckets: 704x1024, 1024x1024, 1024x704"
   DatasetTab.mojo:63; LoRA "Targets: qkv, proj, mlp, modulation" LoraTab.mojo:57;
   almost every aux/window surface in §1). The aux windows in particular are
   Rust-snapshot parity stubs full of hardcoded field text and are not wired to
   the running app (imported only by the compile test
   `tests/ui/test_aux_screens_compile.mojo`).

7. **sd35 blocked route.** model_type STABLE_DIFFUSION_35 (index 3) has no
   architecture entry; `_arch_index_for_model_type` returns -1
   (TrainerConfigModel.mojo:786), so `trainer_ui_apply_model_preset` routes to
   `backend_target="sd35"`, `model_arch="sd35"` and returns early
   (TrainerConfigModel.mojo:826-829). At launch, sd35 matches no wired-runner
   guard, so `trainer_ui_launch_live_runner` logs "no live trainer runner wired
   for backend: sd35" and status "No trainer wired for sd35" and returns False
   (TrainerRuntimeBridge.mojo:551-556) — fail-loud, no silent fallback to the
   previous model. (Consistent with the frozen-sd3/sd35 mandate.)

8. **Resume not implemented** — see §4 (MJ-1077): the argv resume slot is always
   `-`; `continue_last_backup` never reaches an argv.

---

## 9. WEB UI MAPPING (bridge, not design)

- Config model (`TrainerUIConfig`) → `GET/PUT /api/config` (JSON == the
  `trainer_ui_config_json_snapshot` schema `serenity.trainer_ui.v1`).
- Preset table (`trainer_ui_apply_model_preset`) → `GET /api/presets` and
  `POST /api/config/preset {architecture_index}` (server applies the preset,
  returns the merged config).
- Architecture/model-type selectors + sync maps → part of `/api/presets` metadata
  (the 13-row table + the arch↔model_type mapping).
- Runner JSON emission (`trainer_ui_runner_train_config_json`) → server-side on
  launch; not a client concern.
- Start → `POST /api/runs {config}` (server validates, writes runner config,
  spawns the trainer process directly — no terminal — returns run_id).
- Stop → `POST /api/runs/{id}/stop`; Pause/Resume/Sample/Save →
  `POST /api/runs/{id}/{action}` (command-file semantics; note most are [not wired]).
- Progress + status rail → `WS /api/runs/{id}/events` streaming the parsed
  step/loss/grad/lr/eta + status; system metrics via the same socket or
  `GET /api/system/metrics`.
- Validation + capability warning → `GET /api/config/validate` returning the
  "Ready"/error + the "<backend> ignores: …" summary.
- Runs list / logs → `GET /api/runs`, `GET /api/runs/{id}/logs`.
- Dataset gallery → `GET /api/dataset/media?path=…` + static file serving of
  images/video thumbnails; lightbox = client-side.
- Sample gallery → `GET /api/runs/{id}/samples` (or `?dir=sample_output_dir`) +
  static serving; poll/stream for new samples during a run.
- Captioner → `GET /api/captioner/models`, `POST /api/captioner/scan {folder}`,
  `POST /api/captioner/run {settings}` (replaces the JSONL command file); sidecar
  read/write via `GET/PUT /api/captioner/caption?path=…`.
- Config snapshot persistence → server-owned; the fixed
  `target/serenity_trainer_ui_config.json` becomes a per-user/server config store.
- Everything under §1 "aux/window surfaces" → future tabs; not needed for parity.
