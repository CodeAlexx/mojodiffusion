# serenitymojo.models.ernie - ERNIE-Image (Baidu single-stream DiT) per-model
# training surface: config (dims + recipe read from JSON), block kind (one
# single-stream GELU-gated block fwd+bwd), weight loader. Mirrors
# serenitymojo.models.klein. Consumed by serenitymojo.training.train_step.
