# serenitymojo.models.sd35 - SD3.5 MMDiT per-model training surface:
# config (dims + recipe), the JointTransformerBlock fwd-save-acts + backward (+LoRA).
# Consumed by the training pipeline; parity-gated under parity/.
