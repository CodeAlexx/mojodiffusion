# serenitymojo.models.qwenimage — Qwen-Image MMDiT per-model training surface:
# config (dims + recipe), the double-stream block kind (fwd saving acts +
# hand-chained bwd + LoRA variants), full-depth stack, LoRA set + AdamW + save,
# weight loader. Consumed by serenitymojo.training.train_step.
