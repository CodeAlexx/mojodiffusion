# serenitymojo.models.pid — NVIDIA Pixel Diffusion Decoder (PiD) modules.
#
# Pure-Mojo + MAX port of the PiD networks (pid/_src/networks/*). Each module is
# unit-gated against a PyTorch reference (random seeded weights) before use.
