# serenitymojo.models.klein - Klein (FLUX.2) per-model training surface:
# config (dims + recipe), block kind (single + double stream), sigma map,
# LoRA targets, weight loader. Consumed by serenitymojo.training.train_step.
