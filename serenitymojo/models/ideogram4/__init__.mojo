# serenitymojo.models.ideogram4 — ideogram-4 training block + LoRA module,
# migrated from serenity-trainer so the autograd_v2 engine (serenitymojo) can
# import the block backward (one-way dep: serenitymojo cannot import serenity-trainer).
