# TODO — the ONE list (central index)

**This file is the single source of truth for "what's open and where the detail lives."**
There are ~75 scattered TODO/HANDOFF/STATUS/PLAN docs across the repos (a session
left one each). Don't read them all — each ACTIVE item below links to its single
authoritative detail doc. Dated `HANDOFF_*` / `NEXT_SESSION_*` / `*_2026-*` files
are HISTORICAL SNAPSHOTS — ignore unless archaeology is needed.

Convention: update the **Active** section here each session (one line per item +
link). Keep the depth in the linked doc, not here. Last updated: 2026-06-27.

---

## 🔴 ACTIVE / OPEN

| # | Workstream | State | Authoritative doc |
|---|-----------|-------|-------------------|
| P | **serenitymojo perf pass** (branch `perf-pass`, NOT merged into krea2) | 6 op-level wins implemented + op/model-gated (conv transpose · RMS+LN d_g O(n²)→O(n) · _colsum tree reduction · swiglu_packed · norm_modulate). Plus ledger corrections (sync=use-after-free, fusion=inference-only, ideogram double-fwd=gradient-checkpointing, MJ-1017 optimizer false-red). Integration tier (fused-AdamW/flash/trim/capture) blocked behind trainer scaffolding (MJ-0804/0805). | `PERF_PASS_2026-06-26.md` + cross-stack ledger `/home/alex/eng-knowledge` (`recall tag=perf`) |
| 1 | **LTX2 inference → trainer** (video+audio, production quality) | PAUSED (GPU handed back). Inference recipe/conditioning gap reproduces in the OFFICIAL pipeline; resume plan staged. | `serenitymojo/docs/LTX2_TODO.md` |
| 2 | **Phase-5 process-isolation-per-model** (daemon) | CPU scaffold DONE + CPU e2e PASS (spawn/IPC/switch/cancel). ONE deferred GPU run: real zimage/qwen children → confirm kill reclaims VRAM + zimage↔qwen no longer OOMs. Not daemon-default until then. | `../serenityUI/PHASE5_PROCESS_ISOLATION_DESIGN.md` |
| 3 | **Z-Image / L2P training** (in-Mojo prepare path) | Pending, AFTER LTX2. 51 alina samples staged at `output/alina_zimage_stage`; need prepare→cache→1-step train gate. | `TRAINING_PLAN_zimage.md` |
| 4 | **serenity-trainer parity campaign** (model-by-model) | Per doc: Chroma/Ernie/Anima/SDXL-stage1 DONE; Qwen/Klein/SDXL-stage2 remain. (Serenity ref deleted — serenitymojo torch parity gates are the oracle.) | `../serenity-trainer/docs/TRAINING_PARITY_CAMPAIGN.md` |
| 5 | **NAVA audio-video** (baidu MMDiT → pure Mojo) | Per doc: oracle up on fp8; chunked parity build + full sampler next. | `serenitymojo/docs/NAVA_INTAKE_PLAN.md` |
| 6 | **OT-parity campaign → V2 ENGINE SWAP (IN PROGRESS — mandate executing)** | 06-11 session 3 (overnight): **Phases A (sync-elim) + C (resident-set) SHIPPED on Z-Image, BIT-EXACT** — B1 2.0-2.1→1.8 s/step, B2 3.5→3.4 s/step = **1.70 s/sample** vs OT 1.05; all gates green (anchors every digit, b1match/b2dup byte-identical, distinct-mean 7 digits). Diagnosis measured: GPU idle 0.56-0.60 s/step from ~1.9k syncs (modvec uploads + per-step lora set rebuild + AdamW 490 MB round trip). Z-Image is now ~kernel-bound (SDPA math-mode). Klein resident-set ALSO SHIPPED: 2.6-2.7→**2.5-2.6 s/step** (optim 0.18→0.081), variance-class gate PASS (Klein's pre-existing ~4e-4 run nondeterminism measured on old binary too; ⚠ klein torch gates still BROKEN at HEAD — anchor-gate only). **THE GRAPH ENGINE ITSELF ALSO SHIPPED same night (P1-P3 + D.1/D.3 + P5 smoke, commits 3243349/dc1de6d/2fc7c6c): serenitymojo/autograd_v2/ dependency-counted engine; zimage B1 backward runs THROUGH the engine (`ZIMAGE_V2_GRAPH`), 100-step BYTE-IDENTICAL to the hand-chain at 1.8 s/step; CUDA-graph capture through MAX MEASURED FEASIBLE (3.6× submit reduction).** Contract: `serenitymojo/docs/AUTOGRAD_V2_MOJO_DESIGN.md` (C1-C15). **P4+P5 ALSO SHIPPED pre-dawn (a6724cb, cad34a7): StepSlab deterministic allocation + CUDA-graph capture/replay — zimage B1 trains via 2 cuGraphLaunch calls (5,774+21,036 nodes), 100-step BYTE-IDENTICAL, 2.0→~1.63 s/step. ENGINE COMPLETE ON ZIMAGE B1.** **P6 Klein graph ALSO SHIPPED (06-11 mid-day session 4, gates run after the overnight session died at disk-full): per-block graphs through the engine (`KLEIN_V2_GRAPH`), same-process bit gate ALL PASS (autograd_v2/tests/klein_block_parity — every grad n_mismatch=0), trainer 3-step anchors inside the variance class, save-state cadence exercised, 2.5-2.6 → 2.2-2.4 s/step.** **SDPA FLASH OP SHIPPED (06-11 session 4, sign-off GRANTED): cuDNN v9 fwd+bwd 35-43x over math-mode, all parity gates PASS (ops/tests/sdpa_flash_parity.mojo); trainer wiring + re-anchor next. P7 groundwork shipped GATED-OFF — measured 24 GB wall (b2 hand-chain peaks 23.4/24; eager ring allocator double-reserves); unblocks after flash wiring + slab-routed B2 fwd.** **KLEIN FLASH WIRED (06-11 evening): 2.2-2.4 → 1.8 s/step (~1.21x to OT), klein_block_parity ALL PASS (dQ-nondeterminism tolerance gates), 4dp anchors unchanged.** **ZIMAGE FLASH WIRED (06-11 night): step ~1.35 s (~1.29x to OT), 4dp anchors hold, capture off (flash>capture).** **IDEOGRAM4 TRAINING FIXED (06-11): old run was a parity-fixture cache + fixed t=0.7 (loss 1.3e-4, grad 0 — learned NOTHING, measured); now real 70-sample giger cache (Mojo VAE+Qwen3-VL prepare, serenity-trainer cad6237) + per-step t~logit-normal(0,1.5) per DiffSynth — 30-step gate: loss 0.6-1.2 class, smooth 1.125→1.059 DECREASING, all 204 LoRA-B nonzero (Σ|B|=65k), 5.4 s/step.** All 8 UI runner binaries rebuilt from HEAD with flash+rpath; UI cache defaults audited (klein+l2p were dangling — fixed). **HIDREAM-O1 VERTICAL SHIPPED (06-11 late): block torch-gated, trainer ~1.0 s/step (98→1.0 measured fix: bf16-resident vs per-visit F32 re-convert), 30-step B-growth gate green.** **TIER-1 SIMPLETUNER-PARITY CAMPAIGN SHIPPED (06-11 night, 6-agent triad, all orchestrator-gated): loss fns (huber/smooth-L1/min-SNR-flow) + EMA + adafactor/schedule-free + caption dropout (all 4 trainers) + masked loss + validation adapter sweeps — modular levers.mojo, runtime-config, UI widgets + runner emission end-to-end; zimage anchors EXACT flag-off; ledger in TIER1_PARITY_CAMPAIGN_2026-06-11.md.** ~~NEXT: lever fan-out → hidream UI target~~ (both DONE 06-11/12 — see rows 7-8). NEXT (06-12): inference speed sweep (#6) → P7 zimage B2 (gated off at the 24 GB wall; flash freed the score buffers, needs slab-routed B2 fwd) → ernie/sdxl/l2p fixes. **IDEOGRAM4 WIRED (06-25): the 3rd trainer on the engine (`IDEOGRAM4_V2_GRAPH` default-ON, Klein-P6 coarse pattern, serenity-trainer `models/ideogram4/block_stack_graph.mojo`); per-block bit gate BIT-EQUAL + live 30-step anchor ON(engine) vs OFF(hand-chain) loss BIT-IDENTICAL (max\|diff\|=0.0); real-run loss drops; ~+23% slower (no slab/capture yet — owed). Same session: ai-toolkit ideogram4 parity campaign COMPLETE (MRoPE inv_freq bug fixed) + P0 recipe fixes (caption-wrap, pad-mask) + eri2 cache re-staged.** | **`serenitymojo/docs/MOJO_V2_ENGINE_PLAN.md`** (phase ledger, AUTHORITATIVE) + `BEAT_FLAME_SCOREBOARD_2026-06-10.md` 06-11 session-3 entry + `HANDOFF_2026-06-11_OVERNIGHT_OT_PARITY.md` (mandate + verify-commands) |
| 7 | **SimpleTuner/OneTrainer feature-parity campaigns (Tier-1 + Tier-2) — COMPLETE 2026-06-11/12** | Tier-1 ALL SIX PHASES SHIPPED (loss fns/EMA/adafactor+SF/caption dropout/masked loss/adapter sweeps, modular `training/levers.mojo`, orchestrator-gated). **Levers fan-out DONE: all FOUR trainers (zimage/klein/hidream/ideogram4) dispatch through the shared module** (mojodiffusion 12190f6 + serenity-trainer da7cbb9; flags-off anchors held per ledger). Tier-2 ALL PHASES SHIPPED: T2.A 8-bit Adam (bnb parity, 17 gates, params ≤2.4e-7) · T2.B fp8-resident HiDream (VRAM 18255→12315 MiB, default-OFF, new numerics class doc'd) · **T2.C zimage full-rank finetune v1 RUNNABLE+gated (c789b6d: 5.31B params = 86%, 67-81 s/step, device peak 15831 MiB)** · T2.D aspect bucketing (SimpleTuner-exact, comptime 14-arm dispatch) · T2.E zimage ControlNet e2e (f17aa20: 46/46 block parity, diffusers save keydiff empty, ~21 s/step v1) · T2.F/F-2 all 7 Lycoris families verified vs pip lycoris 3.4.0 (upstream loads BIT-EXACT) · **T2.G SimpleTuner-LoKr FULL parity — LoKr e2e TRAINABLE on klein (7ea52ed: adapter_algo=4, init_lokr_norm exact port, upstream load bit-exact)**. Skills updated same day (d3c8532). OPEN follow-ups (from the ledgers): structured-kron GPU kernels (full_matrix @9B targets=all ≈17GB dense carriers, preflight fails loud); GPU-resident streamed 8-bit optimizer (T2.C step ~90% host optimizer); T2.C surface→100% + resume sidecar; device-resident ControlNet AdamW (~18 of 21 s); fp8 promotion needs Alex's sample-quality eyeball; LoKr resume/init_lora/EMA. | `serenitymojo/docs/TIER1_PARITY_CAMPAIGN_2026-06-11.md` + `TIER2_PARITY_CAMPAIGN_2026-06-11.md` (ledgers, AUTHORITATIVE) + `AUDIT_SIMPLETUNER_PARITY_2026-06-11.md` |
| 8 | **serenity-trainer UI hardening — waves 1+2 SHIPPED 2026-06-12; wave 3 OPEN** | Wave 1 (f741b9f): terminal lifecycle fixed (kill-pgid never worked — measured; wrapper TERM trap + auto-close; silent gnome-terminal spawn failures now block+fail loud), sampler gallery actually refreshes (was loaded exactly once), per-frame O(n²) log re-read killed. Wave 2 (aa0e2cf): **klein UI launches now go to the REAL serenitymojo `train_klein_real`** (legacy KleinLiveTrainer was hardwired MSE/AdamW, zero levers — measured by grep+strings on the deployed binary); capability table = klein/zimage/hidream/ideogram4 consume levers, chroma/ernie/anima/sdxl/l2p consume NONE → LOUD pre-launch warning naming ignored keys; decorative widgets labeled [not wired] + gate-proven excluded from emission; Max Steps editable. 4/4 UI tests + runner seam gate ALL PASS. OPEN (wave 3): pause/sample-now command-file protocol no trainer reads yet; config LOAD (save-only today); clipboard FFI; rebuild klein runner post-T2.G. | serenity-trainer `docs/UI_AUDIT_2026-06-12.md` + commits f741b9f/aa0e2cf |

| 9 | **Trainer driver separation → serenity-trainer (2026-06-23)** | DONE pending commit. All 13 `train_*_real` drivers build from `serenity-trainer` (skeptic-verified); 23 driver/smoke files `git rm`-ed from `serenitymojo/training` (STAGED, not committed); 16 `scripts/check_*` gates repointed + re-run clean (0 deleted-file refs); inference `zimage_generate` still links. zimage real-step proven; other 10 build-only (convergence/samplers NOT verified). | `../serenity-trainer/docs/TRAINER_DRIVER_SEPARATION.md` |
| 10 | **Mojo ideogram4 training — ai-toolkit parity (2026-06-25)** | **Every per-component gate PASSES vs ai-toolkit** (prior gating was vs the INVALID ideogram4-ref): block fwd+bwd ≥0.99998 · full forward→velocity ≥0.99979 · MRoPE · VAE encode 0.99995 · Qwen3-VL encoder taps 0.99994 · step-math/recipe MATCH. **Found+FIXED a real bug**: MRoPE `inv_freq` bf16→f32 (`ideogram4_mrope.mojo:49`, KNOWN_ISSUES MJ-0815) — affects training+inference. **COMPLETE 2026-06-25:** P0 fixes (padding-mask + caption-wrap) applied+re-verified (token-ids identical to ai-toolkit; cache re-staged, pad-rows=0); **real-run verdict PASS** (loss drops + 8.4% sample shift, 81% pixels changed); **autograd_v2 WIRED** (`IDEOGRAM4_V2_GRAPH` default-ON, per-block bit gate + live anchor BIT-IDENTICAL). Optional: P1 LR-default, slab/capture speed (engine ~+23% s/step coarse stage-1). | `serenitymojo/models/ideogram4/parity/IDEOGRAM4_AITOOLKIT_PARITY_LEDGER.md` |
| 11 | **Mojo krea2 LoRA training port — ai-toolkit parity (2026-06-25)** | Port ai-toolkit krea2 training to pure Mojo (oracle=ai-toolkit, SingleStreamDiT 28 blocks, Qwen-Image VAE + Qwen3-VL-4B). **Phases 1-3 lead-verified cos ~1.0** (block fwd+bwd+LoRA; stack LoRA backward final→×28 frozen-skip; data cache — latent normalize **cos 1.0 vs ai-toolkit**, 3 dtype bugs fixed). **4a: streaming trainer RUNS end-to-end** on real data (finite loss, LoRA grads nonzero+growing) — mixed-precision bf16 backward resolved (5 rms_norm+3 modulate casts, F32 gates still bit-identical). **Verdict CONFIRMED, NO BUG (lead-run): fixed-objective overfit → loss drops MONOTONICALLY 0.106→0.092/12 steps, grad_norm grows = textbook LoRA.** The flat production-loss was the per-step sigma+noise RESAMPLE (noisy objective) + B=0-init; the "30000× < ideogram4 grad" was a FALSE ALARM (that was a B-weight L1, not a grad norm; real oracle grads ernie/anima 0.0008/0.0015, krea2 in-band). **LT BUCKETING DONE** (pad to LTMAX=768 + **flash-padmask** masked attn via cuDNN `real_len` tail-mask; `[TXT_real\|IMG\|TXT_pad]` reorder since cuDNN padmask is prefix-validity) → multi-sample fits; masked-pad gate PASS cos≥0.9999989 (value-tol, flash dQ). **ZERO PER-STEP DISK READ** (was re-reading 28 bf16 blocks ~48GB/step=~150s/step): **fp8-resident base** (28×8 matmuls quantized ONCE ~12GB, dequant per block) + **resident conditioning** (txtfusion+embedders loaded once) → **~150s→~65s/step**, vram stable. Gates (lead): fp8 round-trip min cos 0.99965; no-pad block parity C13 cos≥0.99999999 (bf16 path unchanged). **SPEED ~65s→~7s/step (~9×, nsys-found): rms_norm_backward_dx** at the 5 FROZEN norms — the discarded d_g kernel was O(rows×cols²) = 89% of the step (computed a frozen-weight grad then threw it away); block parity UNCHANGED cos≥0.99999999. matmuls were always tensor-core, flash always cuDNN. **NEXT (workstream A, "b then a"): toward ai-toolkit ~3s/step** — nsys-measured the ~7s is the EAGER per-op efficiency gap to torch (GEMMs ~49% of peak + ~3200 unfused bias/cast/memory ops/step + full-recompute), NOT a graph (ai-toolkit is eager 2-3s). Biggest lever = **activation-offload to skip the recompute** (~2.5s; ~25GB acts async to host, turbo_loader-style); then matmul-backend (49%→65%) + op-fusion. Best-case ~3.5-4s. KNOWN_ISSUES MJ-0829. **4b autograd_v2 (2026-06-26, user "aim higher — klein 2.2s/zimage faster prove the backend reaches top speed via engine+slab+capture"):** pursued it — **Phase 1 fine-grained engine arm BIT-EXACT** (KREA2_V2_GRAPH; OPK_REPEAT_KV/SIGMOID for GQA+sigmoid-gate; C15 balanced-fold fork measured 832k/3.1M; committed 0c554e6 + bf16 norm-cast d828c6f) **but capture BLOCKED on 24GB: fine-grained slab one-block peak MEASURED ~20GB at L=4864** (10.05GB@L=2432, bump-alloc no per-node freeing) — can't co-fit the 12GB fp8 base. Needs **per-node slab freeing (engine change)** — MJ-0917. Device-grad+slab Phase 2 reverted (device-grad-no-slab leaked the default). **Banked at the 9× + the bit-exact engine arm.** **Speed (2026-06-26, user "OneTrainer 2.xx/ai-toolkit 3.xx — 7s not the limit; host-bound, kill to_host"):** MEASURED **COMPUTE-bound** (clean dmon: mean SM 91.6%, GPU<50% only 5%, mem~0). to_host kill (KREA2_DEVICE_LORA_GRAD, 224→28 syncs, fits 24GB, committed 42a4d05) external-wall A/B (12 steps) = **-0.14s/step → host-bound REFUTED.** ai-toolkit RECOMPUTES too (mmdit.py:446) + keeps grads device. Kernel breakdown (pre-fix nsys minus rms_norm bug): **GEMM 63% (mlp 4864×16384×6144 ~49% peak), flash 22%, elementwise 9.4%, bias 2.5%.** Dominant ~3s lever = GEMM efficiency (49%→70%) = **MAX cutlass-config for krea2's shapes, BELOW the trainer layer** (same ceiling the slab hit). Trainer-addressable ~5-6% = fusion B4-B6 (small ops bandwidth-bound: vec2 rms_norm only 1.05×, gated b70f662) + bias-fusion. nsys post-fix conversion bugged + ncu ERR_NVGPUCTRPERM. MJ-0829. **LoRA SAVE DONE (MJ-0805, 2026-06-27 f3db441): real training USABLE end-to-end** — added save_krea2_lora -> 224-pair re-loadable PEFT (exact ai-toolkit keys, lead-verified bytes: 448 tensors, 28 blocks x 8 slots, correct shapes), periodic+final save, io/ffi mkdir, workspace_dir. Real 512px: 20 steps ~4.2s/step, 224 pairs/107MB. 512px needs LTMAX=896 (70-sample giger captions->803, x128); 1024px default keeps 768. SCOPE main-block only. **LoRA INFERENCE WORKS (2026-06-27, aa0d83e): train→save→load→inference→VISIBLE SHIFT** (A vs B pixel diff mean_abs 1.66, 15% px = DIFFER). fp8-resident inference (killed per-step 26GB disk re-stream, ~28→~7.5min) + LoRA-apply overlay (--lora) + OOM fix MJ-1019 (per-block sync drains transients, vram flat). Still ~7.5min (tiled-math attn O(L²)); flash-wire (real_len=L→cuDNN flash) in flight for ≤4min. perf-pass merged 035ab8a. LoRA = 8 nn.Linear/block (mod.lin a Parameter, NOT wrapped). | `serenitymojo/models/krea2/parity/KREA2_TRAINING_PARITY_LEDGER.md` |
| 12 | **LyCORIS make-all-families-trainable + OneTrainer re-target (2026-06-27)** | Extends T2.F/G (row 7). **ai-toolkit (lycoris 1.8.3, NOT the 3.4.0 T2.F used) verified**: LoKr/LoHa/LoCon reconstruction == Mojo formula EXACT (MJ-1020). **Additive-family (a,b)-carrier DISPATCH proven e2e on real Klein-9B**: LoKr (MJ-1021) + **LoHa NEW carrier `r_eff=r²`** (`loha_stack.mojo`, MJ-1022) — fwd+all 144 d_A/d_B FINITE, master AdamW moves the zero-leg, `save_klein_{lokr,loha}`. LoCon-linear≡LoRA. **DoRA re-targeted to OneTrainer** (per-INPUT axis `wd_on_out` flag; `dora_onetrainer_parity` cos=0.99999998; lycoris path no-regress; MJ-1023 fixed). **OFT re-targeted to OneTrainer** (`oft_onetrainer.mojo` 5-term Neumann; fwd cos=0.99999999999999, backward FD cos≥0.999999999; MJ-1024). **DoRA fwd+bwd** verified vs OneTrainer (incl. autograd backward; MJ-1023). **BOFT SKIPPED** (user; OneTrainer + ai-toolkit-1.8.3 both LACK boft/oft/dora; MJ-1025). **DoRA + OFT now trainable via the carrier as FULL deltas** (`a=I`, `b=W_eff−W`, `r_eff=in`; `dora_stack.mojo`/`oft_stack.mojo`; gates EXACT/cos≥0.99999998; MJ-1026) — VRAM-bound at klein scale (preflight), usable small/subset. **ALL ACTIVE FAMILIES (LoKr/LoHa/DoRA/OFT) HAVE CARRIER DISPATCH.** **PRODUCTION: LoHa now wired into the LIVE klein trainer** (serenity-trainer `train_klein_real`, branch `loha-live-dispatch`; builds clean -O2; carrier_active generalization; MJ-1027) — LoKr was already wired. **zimage LoKr+LoHa ported** (2nd trainer, `models/zimage/zimage_{lokr,loha}_stack.mojo` + orchestration smokes PASS; MJ-1028). OPEN: LoHa real-data train run (final confirm) + DoRA/OFT klein-set orchestration before wiring + carrier-through-zimage-stack real smoke + klein-scale W_eff path (deferred). | `serenitymojo/docs/LYCORIS_CARRIER_DISPATCH_2026-06-27.md` |

> Items 4–5 statuses are quoted from their docs/memory, not re-measured this session.

---

## 🟢 RECENTLY DONE (this session, 2026-06-10/11)

- **2026-06-11/12 (one day):** flash SDPA op+wiring (klein 1.8 / zimage ~1.35 s/step) ·
  hidream-o1 vertical 98→~1.0 s/step · ideogram4 real-training fix · Tier-1 campaign
  (6 phases) · levers fan-out (4 trainers) · Tier-2 campaign (8-bit Adam, fp8-resident,
  full-FT v1, bucketing, ControlNet e2e, 7 Lycoris families, LoKr e2e) · UI hardening
  waves 1+2. Rows 6-8 above carry the numbers; ledgers carry the gates.
- **Gen-screen parity campaign** — pure-Mojo SwarmUI clone. Phases 1–4 skeptic-FIT;
  F1/F2 fixed; **F3 post-OOM recovery MEASURED-PASS**; UI↔daemon `--selftest` ALL
  PASS. → `../serenityUI/SERENITYUI_TODO.md`, `../serenityUI/GENSCREEN_PARITY_PLAN.md`
- **Phase-5 scaffold** (see Active #2) — built + CPU-verified this session.

---

## 📁 DETAIL-DOC INDEX (by area — the current authoritative one per topic)

**Gen screen / daemon / UI**
- `../serenityUI/SERENITYUI_TODO.md` — gen-screen campaign status + Phase 4/5 verdicts
- `../serenityUI/GENSCREEN_PARITY_PLAN.md` — phase structure
- `../serenityUI/PHASE5_PROCESS_ISOLATION_DESIGN.md` — process isolation design + CPU results
- `../serenityUI/MODEL_WIRING_STATUS.md`, `../serenityUI/SWARMUI_GAP_AUDIT.md`, `../serenityUI/DAEMON_BRIDGE_SPEC.md`

**LTX2 (video/audio)**
- `serenitymojo/docs/LTX2_TODO.md` — resume plan (AUTHORITATIVE)
- archive: `serenitymojo/docs/LTX2_*HANDOFF*`, `LTX2_*_2026-06-04.md` (root + docs)

**Training (per-model + ports)**
- `../serenity-trainer/docs/TRAINING_PARITY_CAMPAIGN.md` — parity campaign (AUTHORITATIVE)
- `TRAINING_PLAN_zimage.md` — Z-Image/L2P (Active #3)
- `FULL_PORT_ROADMAP.md` — overall port roadmap
- `serenitymojo/docs/PORT_GAP_AND_PLAN_2026-06-03.md` — port gap campaign
- per-model plans: `TRAINING_PLAN_{flux,sdxl,ernie,anima,anima_OT}.md`
- status snapshots: `serenitymojo/docs/{SDXL_FLUX_KLEIN_PORT_STATUS,TRAINER_STATUS_2026-06-04,IDEOGRAM4_STATUS}.md`

**NAVA / other models**
- `serenitymojo/docs/NAVA_INTAKE_PLAN.md` (AUTHORITATIVE), `serenitymojo/docs/NAVA_HANDOFF_2026-06-07.md`

**MOJO-libs / MojoUI**
- `../MOJO-libs/CHAT_UI_TODO.md`
- `../MojoUI/HANDOFF_MOJOUI_PRODUCTION_READINESS_2026-06-04.md`

**Archive (historical snapshots — do not treat as open work):**
`HANDOFF_2026-05-*`, `NEXT_SESSION_STATE_2026-05-31*`, `*_PLAN_2026-05-3*`,
`AUDIT_*_2026-05-30.md`, and the dated handoffs under `serenitymojo/docs/` and
`serenity-trainer/docs/`. They captured a moment; the Active table supersedes them.

---

## How to keep this the ONE list
1. New open work → add a row to **Active** with a link to its detail doc (create the
   detail doc if needed; don't dump depth here).
2. Finish something → move it to **Recently Done** (trim older entries periodically).
3. Writing a new HANDOFF/STATUS for a session? Fine — but add/refresh its **Active**
   row here so this stays the entry point. A handoff nobody can find = a lost handoff.
