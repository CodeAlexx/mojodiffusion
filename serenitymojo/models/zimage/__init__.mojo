# serenitymojo.models.zimage - Z-Image per-model training surface:
# config (dims + recipe), block kind (single-stream modulated), sigma map,
# LoRA targets, weight loader. Consumed by serenitymojo.training.train_step.
